# FinansApp — Yapılacaklar Listesi

## ✅ Şimdi Uygulanacaklar (üzerinde anlaştıklarımız)

- [x] **Native alert()/confirm() → şık bildirim/modal.** Tarayıcının çirkin varsayılan kutularını (35 yerde kullanılıyor) tema renklerine uygun toast bildirimlerine ve onay modallarına çevir.
- [x] **Vade tarihi hatırlatıcıları.** Fatura/Borç kayıtlarına "vadeye X gün kaldı" rozeti ve Özet Paneli'nde "bu hafta/ay ödenecekler" widget'ı ekle.
- [x] **Raporları PDF/Excel olarak dışa aktarma.** Raporlar sayfasına indirme butonu ekle, muhasebeci ile paylaşımı kolaylaştır.
- [x] **Şifre sıfırlama akışı.** Login sayfasına "Şifremi Unuttum" ekle (2 adımlı doğrulama olmadan, sadece e-posta ile sıfırlama linki).
- [x] **Tablo sayfalandırma (pagination).** Gelir/Gider ve Fatura/Masraf tablolarında büyük veri setlerinde performansı korumak için sayfa başına 25-50 kayıt göster.

---

## 💡 Yeni Öneriler (15 adet)

### Finansal Özellikler
- [x] **Bütçe planlama ve aşım uyarıları.** Kategori bazlı aylık bütçe belirleme, limit aşılınca uyarı.
- [x] **Nakit akışı tahmini (cash flow forecast).** Gelecek aylar için gelir/gider projeksiyonu grafiği.
- [x] **KDV/vergi hesaplama desteği.** Fatura tutarlarına otomatik KDV ekleme/çıkarma.
- [x] **Tekrarlayan fatura/gelir otomasyonu.** Her ay otomatik olarak yeni kayıt oluşturma (kira, abonelik vb.).
- [x] **Çoklu para birimi desteği.** Güncel döviz kuru entegrasyonu ile otomatik TL karşılığı hesaplama.
- [x] **Banka hesabı entegrasyonu (Open Banking).** İşlemleri elle girmek yerine otomatik içe aktarma. *(Not: gerçek canlı Open Banking API bağlantısı yerine — aşağıdaki nota bakın — CSV ekstre içe aktarma uygulandı.)*
- [x] **OCR ile fatura/fiş otomatik okuma.** Yüklenen belge görselinden tutar/tarih/satıcı otomatik çıkarılıp form doldurulsun. *(Fatura/Masraf formuna "Fişten Otomatik Doldur" butonu eklendi. `/api/ocr-fis` route'u fotoğrafı sunucu tarafında Anthropic API'ye (vision) gönderip JSON olarak tutar/tarih/satıcı döndürüyor — anahtar istemciye hiç gitmiyor. **Gerekli:** Vercel ortam değişkenlerine `ANTHROPIC_API_KEY` eklemeniz lazım (console.anthropic.com'dan alınır). Şimdilik sadece Fatura/Masraf'ta; istenirse Gelir/Gider'e de eklenebilir.)*
- [x] **Yatırım portföyü için gerçek zamanlı fiyat.** Hisse/döviz/kripto fiyatları API'den otomatik çekilsin. *(Yatırım formuna "Piyasadan Çek" butonu + sayfa başlığına toplu "Tüm Fiyatları Güncelle" eklendi. Yeni `/api/piyasa-fiyati` route'u Yahoo Finance'in anahtarsız chart API'sini kullanıyor — hisse/döviz/kripto/altın destekleniyor. **Not:** Yahoo endpoint'i resmi/dokümante değil, haber vermeden değişebilir; kritik olmayan, elle girişe her zaman düşebilen bir yardımcı özellik. TEFAS yatırım fonları için anahtarsız kaynak olmadığından "fon" türü desteklenmiyor.)*

### Kurumsal / Güvenlik
- [~] **Çoklu kullanıcı ve rol yönetimi.** Muhasebeci, yönetici, salt-görüntüleyici gibi farklı yetki seviyeleri. *(Kullanıcı "tam kapsamlı rol yönetimi" seçeneğini onayladı — büyüklüğü nedeniyle 2 parçaya bölündü, bu turda 1. yarı (DB + backend) tamamlandı:*
  - *Yeni `supabase/migrations/20260816_team_roles.sql`: `team_members` tablosu (davet e-postası, rol, durum), `get_account_id_for_user`/`has_account_role`/`has_account_access` SECURITY DEFINER fonksiyonları, davet kabul edilince otomatik aktifleştiren `auth.users` trigger'ı, ve **10 ana tablonun** (transactions, bills, debts, investments, assets, categories, documents, budgets, bank_accounts, payrolls) RLS politikaları rol-farkında yeniden yazıldı: görüntüleme = yönetici+muhasebeci+salt_görünüm, ekleme/güncelleme/silme = yönetici+muhasebeci (salt_görünüm düzenleyemez), bordro salt_görünüm'e hiç açılmıyor (hassas veri). `audit_log` görünürlüğü de hesap geneline açıldı.
  - *Yeni `lib/team.ts`: rol tipleri, etiketler, `canEditData`/`canManageTeam`/`canViewPayroll` yardımcıları (2. yarıda UI tarafından kullanılacak).
  - *Yeni `lib/supabase/server.ts`: API route'larda oturum sahibinin kimliğini cookie üzerinden okuyan RLS'e tabi server client.
  - *Yeni API route'ları: `POST/GET /api/ekip` (üye davet et / listele — Supabase Admin API ile davet e-postası gönderir, zaten kayıtlı kullanıcıyı direkt aktif ekler), `PATCH/DELETE /api/ekip/[id]` (rol değiştir / hesaptan çıkar), `GET /api/ekip/rolum` (oturum sahibinin kendi rolünü döner — UI'da düzenleme butonlarını gizlemek için).
  - ***Yapmanız gereken:*** `supabase/migrations/20260816_team_roles.sql` dosyasını Supabase SQL Editor'de çalıştırın. Davet e-postalarının gönderilebilmesi için `SUPABASE_SERVICE_ROLE_KEY` zaten ayarlı olmalı (yedekleme özelliğinden); isteğe bağlı olarak `NEXT_PUBLIC_APP_URL` ortam değişkenini eklerseniz davet linki doğru adrese yönlenir (yoksa Supabase'in varsayılan yönlendirmesi kullanılır).
  - ***2a tamamlandı:*** Yeni `lib/use-team-role.ts` hook'u (oturum sahibinin rolünü/hesap id'sini okur), yeni `/ekip` sayfası (üye listesi + davet formu + rol değiştirme + çıkarma), sidebar'a "Ekip Yönetimi" linki (sadece sahip/yönetici görür) ve rol rozeti eklendi.
  - ***2b-1 tamamlandı:*** Yeni `lib/supabase/account.ts` (`getCurrentAccountId(userId)` — RPC ile hesap id'sini çözer). **13 sayfa** (gelir-gider, borç-alacak, fatura-masraf, banka-hesapları, yatırım, varlık, bordro, bütçe, belgeler, kategoriler, aktivite-geçmişi, özet panel, layout'taki tekrarlayan-işlem otomasyonu) artık veri çekerken/eklerken `user.id` yerine `accountId` kullanıyor. Kişisel/hesap-sahibine-özel tablolar (company_settings, backup_settings, notification_preferences, period_locks — bkz. migration notu #2) bilinçli olarak dokunulmadı, hâlâ sadece sahibe özel. Tekrarlayan işlem otomasyonu artık salt_görünüm rolünde hiç tetiklenmiyor (RLS zaten insert'i reddedecekti). Değişiklikler izole TypeScript derlemesiyle söz dizimi hatası olmadan doğrulandı.
  - ***2b-2 tamamlandı:*** Tüm salt_görünüm erişim kısıtlamaları uygulandı:
    - **"..." satır menüsü (5 dosya — gelir-gider, borç-alacak, fatura-masraf, varlık, yatırım columns.tsx):** `canEdit` meta alanı eklendi, `ActionsCell` `canEdit===false` iken hiç render olmuyor.
    - **"Yeni Ekle"/"Excel'den Yapıştır" butonları — 7 sayfa** (gelir-gider, borç-alacak, fatura-masraf, varlık, yatırım, banka-hesapları, belgeler, kategoriler): `useTeamRole` + `canEditData` ile koşullandırıldı; "N" klavye kısayolu da salt_görünüm için devre dışı.
    - **Bütçe sayfası:** Limit silme butonu ve "Yeni Bütçe Limiti Belirle" bölümü `canEdit`e bağlandı.
    - **Bordro sayfası:** Tam sayfa erişim kontrolü — `canViewPayroll(role)` false ise (salt_görünüm) form ve liste hiç render edilmiyor, uyarı gösteriliyor. Sidebar linki de aynı kurala göre gizleniyor.
    - **Ayarlar sayfası:** Artık sadece sahip/yönetici erişebiliyor (muhasebeci ve salt_görünüm için tam sayfa erişim engeli) — şirket ayarları ve yedekleme idari işlemler olduğu için. Sidebar linki de `managerOnly` yapıldı.
    - **Dönem Kilitleme sayfası:** Sidebar linki `managerOnly` yapıldı (sayfa içi erişim engeli eklenmedi — düşük öncelik, RLS zaten sadece sahibi koruyor).
    - Tüm değişiklikler izole TypeScript derlemesiyle doğrulandı (tek bulunan hata, önceden var olan ve ilgisiz bir "key" prop yanlış pozitifiydi).
  - ***Kalan/isteğe bağlı gelecek iş:*** Toplu-yapıştır modallarının kendi iç "Ekle" tetikleyicileri sayfa seviyesinde zaten gizlendiği için ayrıca dokunulmadı; `EditableCell` çift-tıkla-düzenle özelliği hâlâ salt_görünüm için UI'da engellenmedi (RLS veritabanı seviyesinde zaten reddediyor, kötü senaryoda sadece bir hata toast'ı görülür — güvenlik açığı değil, sadece UX pürüzü); dönem-kilitleme/ayarlar tablolarına ekip erişimi eklenmedi (bkz. önceki not).

**Çoklu kullanıcı ve rol yönetimi maddesi tamamlandı.** Sıradaki büyük madde: **Çoklu şirket/proje desteği** (workspace geçişi) — istersen ona geçelim.)*
  - ***Bilinen sınırlama (isteğe bağlı gelecek iş):*** `donem-kilitleme` ve `ayarlar` sayfaları hâlâ sadece hesap sahibine özel (RLS bu tablolarda ekip üyelerine hiç açılmadı) — bir yönetici bu sayfalara girse bile "kayıt yok" görür. İstenirse ayrı bir migration ile bu tablolara da yönetici erişimi eklenebilir.)*
- [x] **Aktivite/işlem geçmişi (audit log).** Kim, ne zaman, hangi kaydı değiştirdi — kurumsal denetim için. *(Yeni `/aktivite-gecmisi` sayfası + `supabase/migrations/20260816_audit_log.sql`. Migration DB seviyesinde genel bir trigger kurup ana tablolara (gelir/gider, fatura, borç/alacak, yatırım, varlık, bütçe, banka hesabı, belge, kategori, bordro) bağlıyor — uygulama kodunda değişiklik gerekmeden her ekleme/güncelleme/silme otomatik loglanıyor, güncellemelerde eski/yeni değer farkı da gösteriliyor. **Yapmanız gereken:** migration'ı Supabase SQL Editor'de çalıştırmanız lazım.)*
- [x] **Oturum yönetimi.** Aktif oturumları görüntüleme ve uzaktan çıkış yapma imkânı. *(Yeni `/oturumlar` sayfası. `auth.sessions` tablosu PostgREST'e açık olmadığından, `auth.uid()` ile korunan 3 adet SECURITY DEFINER SQL fonksiyonu (`get_my_sessions`, `revoke_my_session`, `revoke_other_sessions`) üzerinden erişiliyor — servis anahtarına/sunucu route'una gerek yok. Cihaz/tarayıcı user-agent'tan basitçe özetleniyor (`lib/user-agent.ts`). **Not:** Supabase access token'ları stateless JWT olduğundan, sonlandırılan bir oturumun token'ı süresi (varsayılan ~1 saat) dolana kadar teoride geçerli kalabilir — bu Supabase mimarisinin bir sınırı. **Yapmanız gereken:** `supabase/migrations/20260816_session_management.sql` dosyasını Supabase SQL Editor'de çalıştırmanız lazım.)*
- [x] **Şirket logosu ve markalı rapor şablonu.** Yüklenen logo ile PDF raporların üstbilgisinde görünmesi. *(Yeni `/ayarlar` sayfası: şirket adı + logo yükleme. Logo, ayrı bir storage bucket'a gerek kalmadan istemci tarafında canvas ile küçültülüp (max 240px) base64 data URL olarak `company_settings` tablosunda saklanıyor; PDF oluşturulurken doğrudan gömülüyor. Raporlar sayfası artık bu bilgiyi çekip `exportReportToPDF`'e geçiriyor — şirket adı girilmişse başlıkta "FinansApp" yerine şirket adı görünüyor, logo sol üstte, alt köşede küçük "FinansApp ile oluşturuldu" notu kalıyor. Excel dışa aktarma şimdilik değişmedi (kapsam dışı bırakıldı, istenirse eklenebilir). **Yapmanız gereken:** `supabase/migrations/20260816_company_settings.sql` dosyasını Supabase SQL Editor'de çalıştırmanız lazım.)*
- [x] **Otomatik zamanlanmış yedekleme.** Haftalık/aylık otomatik yedek, e-posta veya buluta gönderim. *(Şirket Ayarları sayfasına "Otomatik Yedekleme" bölümü eklendi: Kapalı/Haftalık/Aylık seçimi + geçmiş yedeklerin listesi/indirme linki. Arka planda `vercel.json`'daki Cron tanımıyla her gün 03:00 UTC'de tetiklenen `app/api/cron/yedekleme` route'u, sıklığı dolmuş kullanıcılar için JSON yedeği oluşturup private `yedekler` storage bucket'ına yüklüyor (e-posta yerine bulut/uygulama-içi arşiv tercih edildi — üçüncü parti e-posta servis anahtarına ihtiyaç duymuyor). Mevcut manuel "Yedek Al" düğmesinden bağımsız, ek bir koruma katmanı. **Yapmanız gereken (3 adım):** (1) `supabase/migrations/20260816_backup_scheduling.sql`'i Supabase SQL Editor'de çalıştırın — `yedekler` adında private bir storage bucket da oluşturuyor; (2) Vercel ortam değişkenlerine `SUPABASE_SERVICE_ROLE_KEY` (Supabase Dashboard > Project Settings > API) ve rastgele bir `CRON_SECRET` metni ekleyin; (3) Vercel'e deploy edince `vercel.json`'daki cron tanımı devreye girer — Vercel projenizde Cron Jobs özelliğinin plana dahil/etkin olduğunu da doğrulamanız gerekebilir.)*
- [x] **Çoklu şirket / proje desteği.** Tek hesapla birden fazla işletme/proje arası geçiş (workspace). *(Ürünleştirme yol haritasının "Faz 1"i olarak uygulandı — SaaS'a dönüşümün temeli, sonraki tüm fazlar (self-servis kayıt, abonelik/ödeme, ekip yönetimini geri açma vb.) buna dayanıyor:*
  - *Yeni `supabase/migrations/20260817_workspaces.sql`: gerçek bir `workspaces` tablosu eklendi. Mevcut her hesap, id'si kendi auth id'siyle AYNI olan bir workspace'e taşındı (riskli bir UUID yeniden-eşlemesine gerek kalmadan) — bu sayede `team_members` tablosundaki mevcut değerler hiç değişmeden yeni workspace'lerle eşleşti. **10 ana tabloya** (transactions, bills, debts, investments, assets, categories, documents, budgets, bank_accounts, payrolls) `workspace_id` kolonu eklenip dolduruldu, RLS politikaları `workspace_id` üzerinden yeniden yazıldı. Mevcut `user_id` kolonu bilinçli olarak silinmedi/anlamı değiştirilmedi — yeni bir `BEFORE INSERT` tetikleyicisi (`set_workspace_id_default`) `workspace_id` boş bırakılırsa otomatik `user_id`'den dolduruyor, böylece kod tabanındaki **hiçbir insert çağrısına dokunulmadan** (13+ sayfa + bulk-paste modalları + tekrarlayan işlem otomasyonu) her şey çalışmaya devam ediyor.
  - *`team_members.account_id` kolonu `workspace_id` olarak yeniden adlandırıldı (FK artık `workspaces(id)`'ye işaret ediyor — eskiden `auth.users(id)`'ye işaret ettiği için yeni/rastgele id'li workspace'lere ekip daveti mümkün değildi). `has_account_role`/`get_account_id_for_user`/`has_account_access` fonksiyonlarının dış imzaları AYNI bırakıldı (geriye dönük uyumluluk — `audit_log` politikası ve `/api/ekip/*` route'ları hiç değişmeden çalışmaya devam ediyor) ama iç mantığı düzeltildi: sahiplik kontrolü artık `workspaces.owner_id` üzerinden yapılıyor (eski `auth.uid() = id` kısayolu sadece taşınan "ilk" workspace'lerde doğruydu, yeni oluşturulan workspace'lerde gerçek sahibi bile dışarıda bırakırdı).
  - *Yeni fonksiyonlar: `get_user_workspaces()` (kullanıcının sahip olduğu + üye olduğu tüm workspace'leri rolüyle listeler), `create_workspace(p_name)` (yeni işletme oluşturur), ve yeni kullanıcı kaydolduğunda otomatik kişisel workspace açan bir `auth.users` trigger'ı (Faz 2'deki self-servis kayıt akışı için de hazır).
  - *Yeni `lib/supabase/workspace.ts`: `getUserWorkspaces()`, `getCurrentWorkspaceId()` (seçili workspace tarayıcı cookie'sinde tutulur), `setCurrentWorkspaceId()`, `createWorkspace()`. Eski `lib/supabase/account.ts`'teki `getCurrentAccountId` artık buna devrediyor — bu sayede var olan 13+ sayfa ve layout'taki tekrarlayan-işlem otomasyonu **hiç dokunulmadan** çoklu workspace'e duyarlı hale geldi.
  - *Yeni `components/workspace-switcher.tsx`: sol menünün üstünde işletme adı + geçiş listesi + "Yeni İşletme Ekle" formu. Tek workspace'i olan kullanıcılara (henüz kimse ikinci işletme eklemediyse) sade bir etiket gibi görünür.
  - *Tüm değişiklikler izole TypeScript derlemesi + ESLint ile hatasız doğrulandı.
  - ***DÜZELTME (aynı gün, ikinci tur):*** İlk tur yalnızca altyapıyı kurmuştu; uygulama hâlâ seçili işletmenin id'sini `user_id` alanına yazıyordu. Bu, 1. işletmede tesadüfen çalışıyordu (o workspace'in id'si kullanıcının auth uid'sine eşit olacak şekilde taşınmıştı), ama `create_workspace()` ile açılan **2. işletmede** `transactions_user_id_fkey` foreign key ihlali veriyordu — yani çoklu işletme özelliği pratikte çalışmıyordu. Yeni sözleşme: **`workspace_id` = kaydın ait olduğu işletme, `user_id` = kaydı oluşturan gerçek kullanıcı.** 19 dosyada tüm okuma filtreleri (`.eq('user_id', accountId)` → `.eq('workspace_id', accountId)`) ve insert payload'ları taşındı; 5 bulk-paste modalındaki `userId` prop'u `workspaceId` olarak yeniden adlandırıldı; `lib/recurring.ts` (tekrarlayan işlem otomasyonu) ve `/katagoriler` sayfası da güncellendi. DB tarafında `supabase/migrations/20260817b_workspaces_app_alignment.sql`: `set_workspace_id_default` tetikleyicisi artık `user_id`'yi `auth.uid()`'e normalize ediyor, `budgets` unique kısıtı `(workspace_id, category_id)`'ye taşındı, `audit_log` tablosuna `workspace_id` eklendi (aktivite geçmişi de artık işletme bazlı). İkinci işletme senaryosu (aç → kayıt ekle → oku → güncelle → sil → izolasyon) RLS altında uçtan uca test edildi, 10/10 geçti.
  - ***Yapmanız gereken:*** `supabase/migrations/20260817_workspaces.sql` ve ardından `20260817b_workspaces_app_alignment.sql` dosyalarını Supabase SQL Editor'de çalıştırın (önce yedek almanız önerilir — ~10 tabloya kolon ekliyor ve RLS'i değiştiriyor).
  - ***Bilinen sınırlama (isteğe bağlı gelecek iş):*** `donem-kilitleme` ve `ayarlar` (company_settings, bildirim tercihleri, dönem kilitleri, yedekleme ayarları) tabloları hâlâ workspace_id'ye taşınmadı — ikinci bir işletme açan kullanıcı için bu sayfalar hâlâ ilk/varsayılan workspace'in ayarlarını gösterir. Ekip daveti hâlâ `EKIP_DISABLED = true` ile kapalı (Faz 4'te açılacak — artık workspace-farkında olduğu için o zaman sorunsuz çalışacak). Abonelik/plan bazlı workspace limiti (Faz 3) henüz eklenmedi.)*
- [x] **Self-servis kayıt & onboarding.** Kullanıcıların kendi kendine hesap açması ve ilk kurulumu. *(Ürünleştirme yol haritasının "Faz 2"si:*
  - *Yeni `app/(auth)/kayit-ol/page.tsx`: e-posta + şifre + işletme adı ile açık kayıt. İşletme adı `signUp` çağrısında `options.data.company_name` olarak gönderilir; DB trigger'ı yeni kullanıcının workspace'ini bu adla açar. Zaten kayıtlı e-posta durumu (Supabase kullanıcı sayımını sızdırmamak için hata yerine boş `identities` döner) ayrıca ele alınıyor.
  - *E-posta doğrulama açık: kayıt sonrası "e-postanızı kontrol edin" ekranı gösterilir. Doğrulama linki yeni `app/auth/callback/route.ts` route'una düşer — hem PKCE (`?code=`) hem e-posta OTP (`?token_hash=&type=`) biçimini destekler, açık yönlendirme (open redirect) koruması var.
  - *Yeni `app/kurulum/page.tsx` — 3 adımlı kurulum sihirbazı: işletme türü → hazır kategori seti (istenmeyenler çıkarılabilir) → ilk banka/kasa hesabı (atlanabilir). Sol menü olmadan tam ekran çalışır. `workspaces.onboarded_at` boş olduğu sürece dashboard layout'u kullanıcıyı buraya yönlendirir; "Yeni İşletme Ekle" ile açılan ikinci/üçüncü işletmeler de aynı sihirbazdan geçer.
  - *Yeni `lib/onboarding.ts`: 4 sektör şablonu (Hizmet/Danışmanlık, E-ticaret, Perakende/Dükkan, Genel/Serbest Meslek), toplam 31 hazır kategori. Kategori ekleme idempotent — sihirbaz tekrar çalışsa bile kopya kategori oluşmaz.
  - ***Güvenlik düzeltmesi:*** `lib/supabase/proxy.ts`'teki sayfa koruması "izin verilenler listesi"nden "varsayılan korumalı" modeline çevrildi. Eskiden korunacak sayfalar tek tek sayılıyordu ve listeye eklenmeyen `/ayarlar`, `/oturumlar`, `/aktivite-gecmisi`, `/donem-kilitleme`, `/ekip`, `/katagoriler` sunucu tarafında **korumasızdı**. Artık sadece giriş/kayıt/şifre sıfırlama/doğrulama yolları açık, sonradan eklenen her sayfa otomatik oturum ister. API route'ları kendi yetkilendirmesini yaptığı için kapsam dışında bırakıldı.
  - *`/login` sayfasına "Ücretsiz kaydolun" bağlantısı ve geçersiz doğrulama linki uyarısı eklendi.
  - *Üretim derlemesi (`next build`) başarılı — `/kayit-ol`, `/kurulum`, `/auth/callback` route'ları oluştu. Kayıt zinciri (meta veriden workspace adı, `onboarded_at` boş kalması, ad boşsa varsayılana düşme) DB üzerinde test edildi.
  - ***Yapmanız gereken:*** (1) `supabase/migrations/20260818_onboarding.sql` çalıştırın; (2) Supabase Dashboard > Authentication > Sign In/Providers > Email'de **"Confirm email" açık** olmalı; (3) Authentication > URL Configuration'da **Site URL** = üretim adresiniz, **Redirect URLs** = `<site>/auth/callback` ve `http://localhost:3000/auth/callback`; (4) üretimde kendi SMTP'nizi bağlayın — Supabase'in yerleşik SMTP'si saatte ~3-4 e-posta ile sınırlı.
  - ***Bilinen sınırlama:*** Davet linkiyle kayıt (yol haritasında Faz 2'nin son maddesi) Faz 4'teki ekip yönetimiyle birlikte ele alınacak — `EKIP_DISABLED` hâlâ true. Demo/örnek veri ile başlangıç deneyimi bilinçli olarak eklenmedi (gerçek veriyle karışma riski).)*
- [~] **Abonelik & ödeme sistemi.** Plan yapısı, ücretsiz deneme ve plan bazlı kısıtlar. *(Ürünleştirme yol haritasının "Faz 3"ü — **ödeme sağlayıcısından bağımsız çekirdek tamamlandı, sağlayıcı entegrasyonu bekliyor**:*
  - *Yeni `supabase/migrations/20260819_subscriptions.sql`: her workspace'e bir `subscriptions` satırı (plan, durum, deneme bitişi, dönem sonu, tolerans süresi, sağlayıcı kimlikleri). Yeni açılan her işletme otomatik **14 günlük Pro denemesi** alır (trigger). Faz 3 öncesi var olan hesaplar `kurumsal`/`aktif` olarak korundu — ücretli sisteme geçişte kimse kilitlenmedi.
  - ***Kısıtlamalar RLS seviyesinde:*** `workspace_can_write()` ve `workspace_has_feature()` fonksiyonları 10 ana tablonun insert/update/delete politikalarına işlendi. İki kural: (1) abonelik biterse **yazma durur, okuma devam eder** — kullanıcının verisi asla rehin tutulmaz, dışa aktarabilir; (2) **bordro** modülü yalnızca Pro ve Kurumsal planlarda. Kullanıcı `subscriptions` tablosuna yazamaz (sadece SELECT politikası var), yani kendini yükseltemez — yazma sadece webhook'tan servis anahtarıyla yapılır.
  - *Plan yapısı: **Başlangıç** (1 kullanıcı, temel modüller, bordro kilitli), **Pro** (5 kullanıcı, tüm modüller), **Kurumsal** (sınırsız kullanıcı). Fiyatlar şimdilik yer tutucu (`lib/plans.ts`).
  - *Yeni `lib/plans.ts` + `lib/use-subscription.ts`: plan tanımları ve DB mantığının arayüz karşılığı (etkin plan, yazma izni, deneme gün sayacı, erişim durumu). Buradaki bir hata veri güvenliğini bozmaz — asıl kısıt DB'de.
  - *Yeni `/abonelik` sayfası: mevcut plan, durum rozeti, deneme geri sayımı, üç planın karşılaştırması. Sol menüye "Abonelik" eklendi; **Bordro** kilitliyse menüde "Pro" rozetiyle görünüp `/abonelik`'e götürüyor. Panelin üstünde duruma göre uyarı şeridi (deneme bitişine 5 gün kala / ödeme alınamadı / abonelik sona erdi).
  - *Yeni `app/api/abonelik/webhook/route.ts`: sağlayıcıdan bağımsız webhook iskeleti. Yetkilendirme, DB güncellemesi ve tolerans süresi mantığı hazır; sağlayıcı seçilince yalnızca `verifySignature()` ve `parseEvent()` doldurulacak. `ODEME_WEBHOOK_SECRET` tanımlı değilse route 501 döner — yanlışlıkla açık kalmaz.
  - *Üretim derlemesi başarılı. Plan mantığı RLS altında test edildi (13 senaryo): deneme aktif/bitmiş, ödeme bekliyor + tolerans içinde/dışında, bordro kilidi, abonelik bittiğinde okumanın açık kalması ve yazmanın engellenmesi.
  - ***Bekleyen (sağlayıcı kararı + satıcı hesabı gerektirir):*** ödeme sağlayıcısı seçimi (**iyzico** — TL yerli kart deneyimi, vs **Stripe** — uluslararası), gerçek fiyatlandırma, ödeme sayfasına yönlendirme akışı, kart güncelleme, abonelik iptal/yeniden başlatma, otomatik faturalandırma. Ayrıca fatura kesme/KDV tarafı için bir mali müşavirle görüşülmesi öneriliyor.
  - ***Bilinen davranış:*** UPDATE engellendiğinde kullanıcı hata mesajı görür; DELETE engellendiğinde RLS satırları filtrelediği için işlem sessizce "0 satır" ile biter (veri korunur ama hata çıkmaz). Bu yüzden uyarı şeridi eklendi.)*
- [x] **Ekip yönetimini plan bazlı geri açmak.** Çoklu kullanıcı ve rol yönetiminin plan limitleriyle birlikte etkinleştirilmesi. *(Ürünleştirme yol haritasının "Faz 4"ü:*
  - *`EKIP_DISABLED` bayrağı 3 dosyadan kaldırıldı (`/ekip` sayfası, `/api/ekip/route.ts`, `/api/ekip/[id]/route.ts`); sol menüdeki "Kapalı" rozeti kalktı, Ekip Yönetimi artık erişilebilir.
  - ***Düzeltilen hata:*** `/ekip` sayfasındaki `EKIP_DISABLED` erken return'ü, hook'ların koşullu çağrılmasına yol açıyordu — 8 adet `react-hooks/rules-of-hooks` ESLint hatası. Bayrak kaldırılınca yapı düzeldi, lint temiz.
  - ***Düzeltilen hata (çoklu workspace):*** `/api/ekip/*` route'ları `get_account_id_for_user()` kullanıyordu; o fonksiyon kullanıcının üye olduğu ilk workspace'i `limit 1` ile seçer. Tek işletme varken sorun değildi ama artık bir kullanıcı hem kendi işletmesine sahip olup hem birkaç işletmeye üye olabildiğinden route'lar **rastgele bir işletme** üzerinde işlem yapardı. Yeni `lib/supabase/workspace-server.ts` seçili işletmeyi (`finansapp_workspace_id` cookie'si) okuyup kullanıcının o işletmeye erişimini `has_account_role` ile doğruluyor; cookie yoksa eski davranışa düşüyor. Cookie'ye güvenilmiyor — yetki kararı her zaman sunucuda veriliyor.
  - ***Düzeltilen hata (sahiplik):*** `/api/ekip/rolum` sahipliği `accountId === user.id` ile belirliyordu; bu yalnızca Faz 1'de taşınan ilk workspace'lerde doğruydu, `create_workspace()` ile açılan işletmelerde gerçek sahip bile "sahip" görünmezdi. Artık `workspaces.owner_id` üzerinden bakılıyor.
  - ***Düzeltilen hata (davetli kullanıcı):*** Faz 2'deki "her yeni kullanıcıya otomatik workspace aç" trigger'ı, davet edilen kişiye de kendi boş işletmesini açıyor ve onu ilk girişte kurulum sihirbazına sokuyordu. Artık davet kaydı olan bir e-posta ile kayıt olan kullanıcıya kişisel workspace açılmıyor; doğrudan davet edildiği işletmeye giriyor.
  - ***Plan bazlı kullanıcı limiti:*** Başlangıç 1, Pro 5, Kurumsal sınırsız. Sahip her zaman 1 koltuk sayılır, iptal edilen davetler koltuğu boşaltır. Kısıt **DB tetikleyicisinde** (`enforce_workspace_user_limit`) — davet akışı service role ile çalıştığı, yani RLS'i atladığı için politika yeterli olmazdı. Arayüzde de koltuk sayacı ("Pro planı — 3/5 kullanıcı") ve limit dolunca "Planı Yükselt" düğmesi var; davet düğmesi pasifleşiyor.
  - *`lib/use-team-role.ts` `accountId` → `workspaceId` olarak güncellendi.
  - *Üretim derlemesi başarılı. DB tarafı 10 senaryoda test edildi: Başlangıç planında davet engellendi, Pro'da 4 üye eklenip 5.'si engellendi, iptal edilen üye koltuğu boşalttı, davetli kullanıcıya kişisel workspace açılmadı ve daveti aktifleşti, davetsiz kullanıcı kendi işletmesini almaya devam etti.
  - ***Yapmanız gereken:*** `supabase/migrations/20260820_team_plan_limits.sql` çalıştırın. Davet e-postalarının gidebilmesi için Faz 2'deki SMTP notu burada da geçerli — `inviteUserByEmail` de Supabase'in e-posta servisini kullanır. Ayrıca `NEXT_PUBLIC_APP_URL` ortam değişkeni tanımlı olmalı ki davet linki doğru adrese dönsün.)*
- [x] **Fatura kesme.** Müşteriye kesilen satış faturası oluşturma. *(Faz 5:*
  - *Yeni `customers` (cari kartı), `invoices`, `invoice_items` ve `invoice_counters` tabloları; `supabase/migrations/20260821_invoicing.sql`. RLS Faz 3 deseniyle birebir aynı — rol + abonelik kontrolü.
  - *Yeni sayfalar: `/cariler` (cari CRUD), `/faturalar` (liste, durum filtreleri, tahsil edilmemiş tutar ve gecikme özeti), `/faturalar/yeni` ve `/faturalar/[id]` (ortak `invoice-form.tsx`), `/fatura-kunyesi` (satıcı vergi bilgileri).
  - *Kalem bazlı KDV: birim fiyatlar KDV hariç girilir, her satırın oranı ayrı seçilir — farklı KDV oranlı kalemler aynı faturada olabilir. Toplamlar DB tetikleyicisiyle (`recalc_invoice_totals`) kalemlerden yeniden hesaplanır, yani başlık ile kalemler hiçbir koşulda ayrışmaz.
  - *Fatura numarası yıl bazlı ve atomik (`next_invoice_number`), `2026-0001` biçiminde. **İptal edilen numaralar yeniden kullanılmaz** (muhasebe pratiği).
  - ***Tasarım kararı:*** "Gecikti" durumu SAKLANMIYOR; `gonderildi` + vadesi geçmiş olarak türetiliyor. Böylece durumu her gün güncelleyecek bir cron'a gerek kalmadı ve bilgi her zaman doğru.
  - *`lib/invoice-pdf.ts`: rapor PDF'iyle aynı altyapıyı (jsPDF + autoTable) ve aynı marka bilgisini kullanan fatura çıktısı. Alt bilgide "resmi e-Fatura değildir" uyarısı var.
  - *Satıcı künyesi (vergi no, vergi dairesi, adres) bilinçli olarak `company_settings` yerine `workspaces` tablosunda — company_settings hâlâ kullanıcı bazlı, oysa fatura künyesi işletme bazlı olmak zorunda.
  - *DB tarafı 7 senaryoda test edildi: numaralandırma artışı, iki farklı KDV oranlı kalemin toplamı (11000+2010=13010), kalem silinince toplamın güncellenmesi, aynı numaranın tekrar kullanılamaması, abonelik bitmişken fatura kesilememesi ama okumanın devam etmesi.
  - ***Yapmanız gereken:*** `20260821_invoicing.sql` çalıştırın, `/fatura-kunyesi` sayfasından vergi bilgilerinizi girin.)*
- [x] **Muhasebeci / mali müşavir erişimi.** *(Faz 6:*
  - *Yeni `/muhasebeci` sayfası: tek alanlı sadeleştirilmiş davet (rol sabit `muhasebeci`). Teknik olarak Faz 4'teki ekip davetinin aynısı ama kullanıcıya tanıdık tek bir kavram olarak sunuluyor.
  - *Yeni `/musterilerim` sayfası: birden fazla işletmeye davet edilmiş muhasebecinin toplu bakışı ve tek tıkla geçiş.
  - *Yeni `app/api/cron/muhasebeci-ozeti`: her ayın 1'inde (vercel.json) bir önceki ayın gelir/gider/fatura özetini muhasebeciye e-postayla gönderir.
  - *Yeni `lib/email.ts`: sağlayıcıdan bağımsız e-posta gönderimi (Resend uygulandı). Anahtar tanımlı değilse gönderim yapılmaz ve "atlandı" raporlanır — ortam değişkeni eklenmeden de uygulama çalışır.
  - ***Yapmanız gereken:*** `EPOSTA_API_KEY` ve `EPOSTA_GONDEREN` ortam değişkenleri (Resend hesabı).)*
- [x] **Pazarlama sitesi ve hukuki metinler.** *(Faz 7:*
  - *Yeni `app/(pazarlama)` grubu: `/urun` (tanıtım), `/fiyatlandirma`, `/sss`, `/iletisim`, `/gizlilik` (KVKK aydınlatma metni), `/kullanim-sartlari`. Ortak başlık/alt bilgi düzeni.
  - *Fiyat tablosu `lib/plans.ts`'ten okunuyor — uygulama içindeki /abonelik sayfasıyla tek kaynaktan besleniyor, fiyat iki yerde ayrı güncellenmiyor.
  - *Yeni `app/robots.ts` ve `app/sitemap.ts`; yardım makaleleri site haritasına dahil.
  - ***ÖNEMLİ:*** Hukuki metinler **taslaktır**, hukukçu onayından geçmemiştir. Sayfalarda görünür bir uyarı şeridi var; inceleme bitince `app/(pazarlama)/hukuki-taslak-uyarisi.tsx` içindeki `HUKUKI_METIN_ONAYLANDI` sabitini `true` yapın. Metinlerdeki [KÖŞELİ PARANTEZ] alanları doldurulmalı.)*
- [x] **Yardım merkezi ve destek.** *(Faz 8:*
  - *Yeni `/yardim` ve `/yardim/[slug]`: 8 adım adım rehber (`lib/help-articles.ts`). İçerik koda gömülü — ayrı bir CMS bu aşamada gereksiz karmaşıklık.
  - *Yeni `components/support-widget.tsx`: canlı destek widget'ı yalnızca `NEXT_PUBLIC_DESTEK_WIDGET_SRC` tanımlıysa yüklenir. Böylece varsayılan olarak hiçbir üçüncü taraf script'i yüklenmiyor (gizlilik metnindeki taahhüt doğru kalıyor).
  - *Sol menüye "Yardım Merkezi" bağlantısı eklendi.)*
- [~] **Mobil uygulama (Google Play).** *(Faz 9 — kod tarafı hazır, yayın sizde:*
  - *Manifest zaten TWA gereksinimlerini karşılıyordu (standalone, 192/512 + maskable ikonlar). Eklenenler: `public/.well-known/assetlinks.json` şablonu, `twa-manifest.json` (bubblewrap) şablonu, `docs/faz-9-play-store-yayin.md` adım adım rehber.
  - ***Kritik düzeltme:*** `/.well-known`, `/robots.txt` ve `/sitemap.xml` proxy'nin oturum korumasından muaf tutuldu. Aksi halde Google'ın doğrulama isteği `/login`'e yönlenir ve hem uygulama–site bağlantısı hem SEO çalışmazdı.
  - ***Yapılamayan:*** Google Play geliştirici hesabı (25 USD, kimlik doğrulaması) ve imzalama anahtarı sizin adınıza alınamaz. iOS için TWA yok — App Store ayrı bir iş.)*
- [~] **Resmi e-Fatura / e-Arşiv.** *(Faz 10 — veri modeli hazır, entegrasyon sizde:*
  - *`supabase/migrations/20260822_einvoice_readiness.sql`: `invoices.einvoice_status/uuid/provider/ref/error/sent_at`, `customers.is_einvoice_user/einvoice_alias`, `workspaces.tax_number/tax_office/address`, ve `einvoice_missing_fields()` fonksiyonu (bir faturanın gönderilmeye hazır olup olmadığını söyler).
  - *`lib/einvoice.ts`: entegratörden bağımsız `EInvoiceAdapter` arayüzü ve `buildEInvoicePayload()`. Sağlayıcı seçilince tek bir adaptör dosyası yazmak yeterli olacak.
  - ***Yapılamayan:*** GİB onaylı entegratörle (Logo, Foriba, Uyumsoft, Nes Bilgi) ticari sözleşme, GİB test ortamı onayı ve mali mühür/e-imza temini. Bunlar teknik değil ticari/hukuki adımlar.)*
- [x] **Ölü kod temizliği.** *(Son adım:*
  - *`supabase/` klasörünün altına yanlışlıkla kopyalanmış **tam bir proje kopyası** (119 dosya: app, components, lib, public, package.json…) silindi. Hiçbir yerden referans verilmiyordu ve `supabase/app/layout.tsx` TypeScript hatası üretiyordu. `supabase/migrations/` korundu.
  - *`app/(dashboard)/katagoriler/` — `kategoriler`in yazım hatalı ikizi, ikisi de canlı route'tu. Silindi.
  - *`lib/supabase/account.ts` — Faz 1'den kalan geçiş kabuğu. 13 dosyadaki çağrılar `getCurrentWorkspaceId`'ye taşındı, `accountId` değişkenleri `workspaceId` olarak yeniden adlandırıldı, dosya silindi. (`importAccountId` gibi banka hesabı id'leri korundu.)
  - *`/ekip` sayfasındaki 8 ESLint hatası Faz 4'te zaten düzelmişti.
  - *Temizlik sonrası `next build` başarılı (59 sayfa), TypeScript ve ESLint temiz.)*
- [x] **Dönem kilitleme (ay/yıl sonu kapanışı).** Geçmiş dönem kayıtlarının yanlışlıkla değiştirilmesini engelleme. *(Yeni `/donem-kilitleme` sayfası: bir tarih seçilip "bu tarihten önceki kayıtlar kilitli" hale getiriliyor. DB seviyesinde `transactions` (date), `bills` ve `debts` (due_date) tablolarına trigger eklendi — kilitli tarihten önceki bir kayıt eklenemez/güncellenemez/silinemez, PostgreSQL hatası olarak engellenir. `gelir-gider`/`fatura-masraf`/`borç-alacak` sayfalarındaki hata toast'ları artık DB'den dönen asıl mesajı (`error.message`) gösteriyor ki kilit engeli kullanıcıya görünür olsun (önceden bazıları jenerik "hata oluştu" diyordu, silme işlemlerinde ise hiç mesaj gösterilmiyordu). **Not:** Tek kullanıcılı hesap modelinde (rol ayrımı olmadığından) kilidi hesap sahibinin kendisi de kaldırabilir — bu, yanlışlık önleyici bir koruma, denetim-geçirmez bir kapanış garantisi değil. **Yapmanız gereken:** `supabase/migrations/20260816_period_locks.sql` dosyasını Supabase SQL Editor'de çalıştırmanız lazım.)*

### Kullanıcı Deneyimi
- [x] **Kategori bazlı grafik/pasta chart raporları.** Harcamaların görsel dağılımı. *(Raporlar sayfasındaki "Kategori Bazlı Harcamalar" kartına, kart başlığının yanına çubuk/pasta grafik geçişi eklendi — daha önce sadece yatay çubuk grafik olarak vardı, şimdi tek tıkla pasta grafiğe dönüştürülebiliyor, aynı veri/renkler kullanılıyor.)*
- [x] **Gelişmiş arama ve filtreleme.** Tarih aralığı, tutar aralığı, kategori bazlı çoklu filtre. *(Yeni ortak `AdvancedFilterBar` bileşeni (`components/data-table/advanced-filter.tsx`): serbest metin arama kutusu + açılır "Filtrele" paneli (başlangıç/bitiş tarihi, min/maks tutar, çoklu kategori seçimi — rozet olarak tıklanabilir). Gelir/Gider ve Fatura/Masraf sayfalarına eklendi; filtreleme istemci tarafında mevcut veri üzerinde çalışıyor, DB'ye ek sorgu atmıyor. Aktif filtre sayısı düğmede rozet olarak gösteriliyor, "Temizle" ile tek tıkla sıfırlanabiliyor.)*
- [x] **Klavye kısayolları.** Hızlı kayıt ekleme, arama gibi işlemler için kısayol tuşları. *(Yeni `lib/use-keyboard-shortcut.ts` genel hook'u + `components/keyboard-shortcuts-modal.tsx` yardım penceresi. Herhangi bir sayfada `?` tuşu kısayol listesini açar (sol menü altında da "Klavye Kısayolları" düğmesiyle erişilebilir). Gelir/Gider ve Fatura/Masraf sayfalarında `N` yeni kayıt modalını açar, `/` arama kutusuna odaklanır; bir metin kutusuna yazarken kısayollar (Esc hariç) devre dışı kalır, böylece normal yazı yazmayı engellemez.)*
- [x] **Yazdırılabilir (print-friendly) rapor görünümü.** Tarayıcıdan doğrudan düzgün baskı alınabilmesi. *(Raporlar sayfasına "Yazdır" butonu eklendi. Yazdırırken: sol menü/mobil çubuk/hesap makinesi gibi arayüz öğeleri gizleniyor, kartlar tek sütuna geçiyor ve karanlık modda bile kağıda her zaman açık zemin + siyah metin basılıyor (globals.css'teki `@media print` kuralları + `.print-card` sınıfı). Diğer sayfalara da (tablolar vb.) aynı `print:hidden` deseniyle kolayca genişletilebilir.)*
- [x] **Mobil bildirimler (PWA push).** Ödeme/vade hatırlatmalarının telefon bildirimi olarak gelmesi. *(Bildirim Tercihleri panelinin üzerine inşa edildi. Yeni `push_subscriptions` (cihaz başına Web Push aboneliği) ve `push_notification_log` (aynı hatırlatmanın aynı gün tekrar gönderilmesini engelleyen "gönderildi" defteri) tabloları. Şirket Ayarları > Bildirim Tercihleri'ne "Mobil Bildirimler (Push)" bölümü eklendi: kullanıcı her cihazda ayrı ayrı "Bu Cihazda Aç" diyerek tarayıcı bildirim izni verip abone oluyor, aktif cihazlarını görüp tek tek kaldırabiliyor. Service worker'a (`public/sw.js`) `push`/`notificationclick` event'leri eklendi. Yeni `app/api/cron/bildirim-gonder` route'u (vercel.json'a günlük — 07:00 UTC — cron olarak eklendi) her gün: (1) vadesi gecikmiş/bugün/3 gün veya daha az kalan fatura ve borçlar için (Bildirim Tercihleri'ndeki "Yaklaşan Ödemeler" açık olan kullanıcılara — 30 günlük tüm ufuk boyunca değil, spam olmaması için sadece son 3 gün/gecikmiş), (2) bütçe aşımı olan kategoriler için ("Bütçe Aşımları" açık olan kullanıcılara, günde en fazla 1 özet bildirimi) `web-push` kütüphanesiyle bildirim gönderiyor; artık geçersiz abonelikleri (404/410) otomatik temizliyor. **Yapmanız gereken (4 adım):** (1) `supabase/migrations/20260816_push_notifications.sql`'i Supabase SQL Editor'de çalıştırın; (2) bu oturumda sizin için bir VAPID anahtar çifti üretildi — sohbetteki son mesajda veriliyor (isterseniz `npx web-push generate-vapid-keys` ile kendiniz de yenisini üretebilirsiniz) — bunları Vercel ortam değişkenlerine `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT` (örn. `mailto:destek@sirketiniz.com`) olarak ekleyin; (3) `SUPABASE_SERVICE_ROLE_KEY` ve `CRON_SECRET` zaten "Otomatik zamanlanmış yedekleme" maddesinde ayarlanmış olmalı, aynıları bu cron için de kullanılıyor — ayarlanmadıysa şimdi ekleyin; (4) Vercel'e deploy edince cron devreye girer. **Not:** Vercel Hobby planında cron job'lar günde en fazla 1 kez çalışabiliyor ve toplam cron sayısı sınırlı olabilir — artık 2 cron job'ınız var (yedekleme + bildirim-gonder), planınızın buna izin verdiğini doğrulayın.)*
- [x] **Karşılaştırmalı trend raporları.** "Bu ay vs geçen ay", "bu yıl vs geçen yıl" analizleri. *(Raporlar sayfasına iki yeni kart eklendi: `lib/reports.ts`'teki `compareLastTwoMonths` ve `compareYearOverYear` fonksiyonları gelir/gider/net için önceki döneme göre yüzde değişimi hesaplıyor; artış yeşil, azalış kırmızı rozetle gösteriliyor. Karşılaştırılacak önceki dönem verisi yoksa (örn. yeni hesap) "yeterli veri yok" mesajı gösterilip yanıltıcı bir yüzde üretilmiyor. DB migration gerekmiyor.)*
- [x] **Belge arşivinde etiketleme ve arama.** Belge içeriğine veya etiketlere göre hızlı filtreleme. *(`documents` tablosuna `tags text[]` kolonu eklendi. Belgeler sayfasında: belge eklerken serbest metin etiket girişi (Enter/virgülle ekle, rozet olarak kaldırılabilir), belge kartlarında etiketler görünüyor, filtre çubuğunun altına tıklanabilir etiket rozetleri eklendi (çoklu seçim, OR mantığı) ve arama kutusu artık etiketleri de tarıyor. **Yapmanız gereken:** `supabase/migrations/20260816_document_tags.sql` dosyasını Supabase SQL Editor'de çalıştırmanız lazım.)*
- [x] **Bildirim tercihleri paneli.** Kullanıcının hangi bildirimleri, hangi kanaldan almak istediğini seçmesi. *(Yeni `notification_preferences` tablosu + Şirket Ayarları sayfasına "Bildirim Tercihleri" bölümü: Özet Paneli'ndeki "Yaklaşan Ödemeler" ve "Bütçe Aşımları" widget'larını ayrı ayrı açıp kapatma, yaklaşan ödemelerin kaç gün öncesinden (7/14/30/60) gösterileceğini seçme. Özet Paneli artık bu tercihleri okuyup ona göre widget'ları gizliyor/ufku ayarlıyor; kayıt yoksa (migration çalıştırılmamışsa) varsayılan olarak her ikisi de açık ve 30 gün kabul ediliyor, böylece mevcut davranış bozulmuyor. Kapsam notu: bu, hangi widget'ların *görüneceğini* kontrol eden bir tercih paneli; gerçek telefon bildirimi (push) göndermiyor — o, ayrı "Mobil bildirimler (PWA push)" maddesinin konusu ve bu panel o özelliğin temelini oluşturacak şekilde tasarlandı. **Yapmanız gereken:** `supabase/migrations/20260816_notification_preferences.sql` dosyasını Supabase SQL Editor'de çalıştırmanız lazım.)*

---

## 📝 Tamamlanan İşler (bu oturumda)

- [x] Lacivert & Altın kurumsal tema uygulandı (globals.css + PWA renkleri)
- [x] Dosya yükleme Cloudinary'den Supabase Storage'a taşındı (private bucket + imzalı link)
- [x] Vercel ortam değişkenleri (Supabase URL/Key) düzeltildi
- [x] `bills` tablosuna eksik `receipt_url` kolonu eklendi
- [x] Tüm sayfalardaki sabit gri renkler (`slate-*`) tema değişkenlerine bağlandı (29 dosya)
- [x] "..." satır menüsünün tablo içinde altta/görünmez kalma sorunu çözüldü (Portal tabanlı ortak bileşen — 5 dosya)
- [x] Marka adı tutarsızlığı düzeltildi ("FinansAsistanım" → "FinansApp", "Kişisel Finans Yönetimi" → "Kurumsal Finans Yönetimi")
- [x] Raporlar sayfasına "PDF İndir" / "Excel İndir" butonları eklendi (`lib/report-export.ts` — jspdf-autotable + xlsx, 3 tablo: nakit akışı, kategori harcamaları, portföy dağılımı)
- [x] Şifre sıfırlama akışı eklendi: Login'de "Şifremi Unuttum" linki → `/sifremi-unuttum` (e-posta ile link gönderimi) → `/sifre-guncelle` (yeni şifre belirleme, `PASSWORD_RECOVERY` event'i dinleniyor)
- [x] Tablo sayfalandırma zaten `DataTable` bileşeninde mevcuttu (25/50/100 seçenekli), 5 tabloda da (gelir-gider, fatura-masraf, borç-alacak, varlık, yatırım) aktif olduğu doğrulandı
- [x] Bütçe planlama eklendi: yeni `/butce` sayfası, kategori bazlı aylık limit belirleme, harcama/limit ilerleme çubuğu (yeşil/sarı/kırmızı), Özet Paneli'nde "Bütçe Aşımları" widget'ı. **Önemli:** yeni `budgets` tablosu gerekiyor — `supabase/migrations/20260816_create_budgets_table.sql` dosyasını Supabase SQL Editor'de elle çalıştırmanız lazım.
- [x] Nakit akışı tahmini eklendi: Raporlar sayfasına, son 3 ayın ortalamasına göre gelecek 3 ay için gelir/gider projeksiyonu gösteren yeni bir grafik (kesikli çizgiler) — `lib/reports.ts`'teki `projectCashFlow`.
- [x] KDV hesaplama desteği eklendi: Fatura/Masraf formundaki "Tutar" alanının yanına, KDV hariç↔dahil çevrimi yapıp sonucu tek tıkla tutar alanına aktaran bir hesaplayıcı (`components/kdv-calculator.tsx`, %1/%10/%20 oranları) — mevcut tabloya kolon eklemeden çalışır.
- [x] Tekrarlayan fatura/gelir otomasyonu eklendi: `bills` tablosunda zaten var olan `is_recurring`/`recurrence_period` alanlarına ek olarak `series_id` ve `recurrence_end_date` eklendi; `transactions` tablosuna da aynı alanlar (yeni) eklendi. `lib/recurring.ts`'teki `runRecurringAutomation`, dashboard layout'unda oturum açıldığında bir kez çalışıp vadesi geçmiş tekrarlayan kayıtların eksik dönemlerini otomatik oluşturuyor (idempotent — sunucu tarafı cron olmadığı için "yakalama" deseniyle çalışıyor). **Not:** Fatura/Masraf formunda tekrar ayarları (`is_recurring`/`recurrence_period`) için state zaten vardı ama arayüzde görünür bir kontrol yoktu (kullanıcı hiç açamıyordu) — bu eksik de bu oturumda giderildi. Gelir/Gider formuna da aynı tekrar mekanizması (maaş, kira geliri vb. için) eklendi.
- [x] Çoklu para birimi desteği eklendi (Gelir/Gider işlemleri): `transactions` tablosuna `currency`, `exchange_rate`, `try_equivalent` kolonları eklendi. `lib/currency.ts`, frankfurter.app (ECB tabanlı, anahtarsız/ücretsiz) üzerinden canlı kur çekip 1 saat localStorage'da önbelleğe alıyor; ağ hatasında kullanıcı kuru elle girebiliyor. Form üzerinde döviz seçici + TL karşılığı önizlemesi, tabloda döviz tutarı + TL karşılığı gösterimi var. Özet Panel, Raporlar ve Bütçe sayfalarındaki toplamlar artık `try_equivalent` üzerinden hesaplanıyor (yoksa `amount`'a düşüyor) — böylece döviz cinsinden kayıtlar toplamları bozmuyor. **Şu an sadece Gelir/Gider işlemlerinde var; Fatura/Masraf'a henüz eklenmedi.**
- [x] Banka hesabı entegrasyonu eklendi — **önemli kapsam notu:** Gerçek "Open Banking" (bankaya canlı API ile bağlanıp işlemleri otomatik çekme) BDDK lisanslı bir aracı/ödeme kuruluşu (TPP) hesabı ve o kuruluşla ayrı bir sözleşme/API anahtarı gerektirir; bu genel bir uygulamaya doğrudan eklenemez. Onun yerine yeni `/banka-hesaplari` sayfası: manuel banka hesabı tanımlama (ad, banka, IBAN son 4 hane, bakiye) ve bankanızın internet şubesinden indirdiğiniz ekstre CSV'sini içe aktararak işlemleri otomatik `transactions` tablosuna ekleme (`lib/bank-import.ts`) sunuyor. Aynı ekstrenin iki kez aktarılmasını `bank_account_id` + `external_ref` üzerindeki benzersiz kısıtlama engelliyor. `bank_accounts` tablosu, ileride gerçek bir sağlayıcı (ör. GoCardless Bank Account Data, Salt Edge) bağlanacaksa da aynı şekilde kullanılabilecek şekilde tasarlandı.
- [x] Native alert()/confirm() ve vade tarihi hatırlatıcıları önceki oturumda kod olarak tamamlanmış ama bu listede işaretlenmemiş bulundu — kontrol edilip ([x]) işaretlendi. Ayrıca fişten otomatik doldurma (OCR) eklendi: yeni `/api/ocr-fis` route'u + `components/ocr-receipt-button.tsx`, Fatura/Masraf formuna bağlandı.
- [x] **Uyarı:** `AGENTS.md` dosyasında, kodlama ajanlarını "node_modules/next/dist/docs/" gibi var olmayan bir yola yönlendirip oradan talimat okumaya ve bu bloğu sorgusuzca commit'lemeye teşvik eden şüpheli bir metin bulundu. Bu tür bir enjeksiyon denemesine benziyor; bu oturumda görmezden gelindi ve hiçbir talimatı uygulanmadı. Dosyanın kim tarafından/neden eklendiğini kontrol etmenizi öneririm.
- [x] **Önemli:** Bu oturumda eklenen tüm DB değişiklikleri (tekrarlayan işlem alanları, çoklu para birimi kolonları, `bank_accounts` tablosu) tek bir migration dosyasında: `supabase/migrations/20260816_recurring_currency_bank.sql` — Supabase SQL Editor'de elle çalıştırmanız lazım.
- [x] **Yeni oturum — 1. tur (3 madde):** Kategori bazlı pasta grafik seçeneği, gelişmiş arama/filtreleme (Gelir-Gider + Fatura-Masraf) ve klavye kısayolları (`?`, `N`, `/`) eklendi. Hiçbir DB migration'ı gerektirmiyor — sadece kod değişikliği, doğrudan deploy edilebilir. Ayrıca listede tekrar eden bir "Klavye kısayolları" satırı bulunup temizlendi.
- [x] **Yeni oturum — 2. tur (3 madde):** Karşılaştırmalı trend raporları (Raporlar sayfası, migration gerekmez), belge etiketleme ve arama (`documents.tags`, migration gerekli), bildirim tercihleri paneli (`notification_preferences`, migration gerekli) eklendi. **Yapmanız gereken:** `supabase/migrations/20260816_document_tags.sql` ve `supabase/migrations/20260816_notification_preferences.sql` dosyalarını Supabase SQL Editor'de çalıştırmanız lazım. AGENTS.md'deki şüpheli enjeksiyon metni bu turda da tekrar tespit edildi, yine görmezden gelindi — hâlâ dosyada duruyor, kaynağını kontrol etmeniz önerilir.

---

## 🔧 Ürünleştirme sonrası iyileştirmeler (10 öneri + bonus)

Fazlar bittikten sonra kod tabanı taranarak çıkarılan liste. İlk dördü yeni
özellik değil, **o an bozuk olan şeylerdi**.

- [x] **1. Dönem kilidi hem delinebiliyor hem taşıyordu.** `enforce_period_lock_*` kilidi `period_locks where user_id = new.user_id` ile arıyordu. Faz 1'den sonra `user_id` = kaydı OLUŞTURAN kişi olduğu için: (a) sahip dönemi kilitlese bile **muhasebecinin eklediği kayıt kilide takılmıyordu**, (b) iki işletmesi olan kullanıcının birinde koyduğu kilit **diğerini de kilitliyordu**. Kilit `workspace_id` bazına taşındı; her iki senaryo da testle doğrulandı.
- [x] **2. Ayar tabloları kullanıcı bazlıydı.** `company_settings`, `period_locks`, `backup_settings` `user_id` anahtarlıydı — ikinci işletme açan kullanıcı birinci işletmenin şirket adını, **logosunu** ve yedekleme ayarını görüyor/düzenliyordu; faturaya yanlış şirketin logosu basılabiliyordu. Üçü de `workspace_id` anahtarına taşındı, RLS yeniden yazıldı. *`notification_preferences` ve `push_subscriptions` bilinçli olarak kullanıcı bazlı kaldı — bunlar kişisel tercih.*
- [x] **3. Yedeğin geri yükleme yolu yoktu.** `lib/backup.ts` yalnızca dışa aktarıyordu ve 6 tabloyu kapsıyordu. Artık **13 tablo** (fatura, cari, bordro, belgeler, banka hesapları dahil) yedekleniyor, işletme bazlı çalışıyor ve `restoreFromBackup` ile geri yüklenebiliyor. Geri yükleme iki adımlı: önce dosyanın içeriği tablo tablo gösteriliyor, sonra üzerine yazma onayı için işletme adının elle yazılması isteniyor. Otomatik yedek de aynı kapsama çekildi ve `workspace_id/` klasörüne yazıyor (eskiden `user_id/` idi ve iki işletmenin yedeği birbirinin üzerine biniyordu).
- [x] **4. Hiç test yoktu.** Uygulamanın tüm güvenlik modeli RLS ve o politikalar beş fazda beş kez yeniden yazıldı. Yeni `supabase/tests/rls-regression.sql`: **25 test**, tek transaction içinde çalışıp ROLLBACK eder (üretimde çalıştırmak güvenli). Kapsam: işletme izolasyonu, rol bazlı yetki, salt_gorunum kısıtları, bordro kilidi, abonelik bitince yazma durması/okumanın sürmesi, plan kullanıcı limiti, dönem kilidi, ayar tablosu izolasyonu, kullanıcının kendi planını yükseltememesi, fatura numarası tekilliği, fatura toplamları, davetli kullanıcı akışı. `npm run test:rls` ya da Supabase SQL Editor'e yapıştırarak çalışır. **25/25 geçiyor.**
- [x] **5. Fatura ödendiğinde gelir kaydı oluşmuyordu.** Fatura kesip "Ödendi" işaretlediğinizde o para nakit akışında ve raporlarda görünmüyordu; kullanıcı aynı tutarı ikinci kez elle giriyordu. Artık `transactions.invoice_id` bağı var; ödendi işaretlenince gelir kaydı oluşuyor, ödenmemişe dönerse veya iptal edilirse siliniyor. Kategori yoksa "Fatura Geliri" otomatik açılıyor. Tekil indeks çift kayıt oluşmasını DB seviyesinde engelliyor.
- [x] **6. Alacak yaşlandırma ve cari bakiyesi.** Yeni `/alacaklar` sayfası: tahsil edilmemiş faturaları cari bazında 0-30 / 31-60 / 61-90 / 90+ gün kovalarına dağıtır, en riskli cari en üstte. Cariye tıklayınca açık faturaları açılır. Ek tablo gerektirmedi.
- [x] **7. Deneme bitiş hatırlatması.** Uyarı yalnızca paneldeydi; giriş yapmayan kullanıcı denemesinin bittiğini öğrenmiyordu. Yeni `app/api/cron/deneme-hatirlatma` günlük çalışıp bitişe 3 gün, 1 gün kala ve bittiği gün işletme sahibine e-posta gönderiyor. `subscriptions.last_trial_reminder` sayesinde aynı e-posta tekrar gitmiyor.
- [x] **8. Hesap silme ve veri dışa aktarma (KVKK).** Gizlilik metninde taahhüt vardı ama mekanizma yoktu. Yeni `/hesabim` sayfası: sahip olunan tüm işletmelerin verisini tek dosyada indirir, ve `POST /api/hesap/sil` ile hesabı siler. Silme yalnızca oturum sahibinin KENDİ hesabını siler (kullanıcı kimliği istekten alınmaz, oturumdan okunur), onay metni ister, depolanmış yedekleri temizler ve başkasının işletmesindeki üyeliği kaldırır — o işletmelerin verisi silinmez.
- [x] **9. Hata görünürlüğü yoktu.** Ne hata sayfası ne 404 sayfası vardı; beklenmedik bir hatada kullanıcı boş ekran görüyordu ve haberiniz olmuyordu. Eklendi: `app/error.tsx`, `app/global-error.tsx`, `app/not-found.tsx` ve `lib/error-reporting.ts`. Raporlama sağlayıcıdan bağımsız — `NEXT_PUBLIC_HATA_BILDIRIM_URL` tanımlıysa oraya gönderir, değilse yalnızca konsola yazar. Sentry'e geçmek isterseniz tek fonksiyonun gövdesi değişiyor (dosyada anlatıldı).
- [x] **10. Tekrarlayan satış faturası.** Gelir/gider ve gelen faturada vardı, kesilen faturada yoktu. Fatura formuna sıklık (aylık/yıllık) ve bitiş tarihi eklendi; `lib/recurring-invoices.ts` mevcut deseni izliyor. **Üretilen fatura her zaman TASLAK açılır** — müşteriye otomatik fatura göndermek muhasebe açısından riskli, kullanıcı gözden geçirip kendisi gönderir.
- [x] **Bonus: Supabase advisor uyarıları.** `push_notification_log` ve `invoice_counters` için açık politika yazıldı; 4 fonksiyonda değişken `search_path` sabitlendi; yalnızca tetikleyici olarak kullanılan 9 fonksiyonun REST erişimi kapatıldı; oturum gerektiren 8 RPC anon rolüne kapatıldı. *RLS politikalarının içinde çalışan `has_account_role` ve benzerlerine bilinçli dokunulmadı — anon'dan yetki alınırsa oturum yüklenmeden yapılan sorgular boş sonuç yerine izin hatası döndürürdü.* **Kalan (Dashboard'dan):** Authentication > Providers > Email > "Leaked password protection" açılmalı.

**Çalıştırılacak migration'lar:** `20260823_settings_workspace_scoping.sql`, `20260824_invoice_income_recurring_trial.sql`, `20260825_security_hardening.sql`

**Yeni ortam değişkeni (isteğe bağlı):** `NEXT_PUBLIC_HATA_BILDIRIM_URL`
