# FinansApp — Yapılacaklar Listesi

## ✅ Şimdi Uygulanacaklar (üzerinde anlaştıklarımız)

- [x] **Native alert()/confirm() → şık bildirim/modal.** Tarayıcının çirkin varsayılan kutularını (35 yerde kullanılıyor) tema renklerine uygun toast bildirimlerine ve onay modallarına çevir.
- [x] **Vade tarihi hatırlatıcıları.** Fatura/Borç kayıtlarına "vadeye X gün kaldı" rozeti ve Özet Paneli'nde "bu hafta/ay ödenecekler" widget'ı ekle.
- [x] **Raporları PDF/Excel olarak dışa aktarma.** Raporlar sayfasına indirme butonu ekle, muhasebeci ile paylaşımı kolaylaştır.
- [x] **Şifre sıfırlama akışı.** Login sayfasına "Şifremi Unuttum" ekle (2 adımlı doğrulama olmadan, sadece e-posta ile sıfırlama linki).
- [x] **Tablo sayfalandırma (pagination).** Gelir/Gider ve Fatura/Masraf tablolarında büyük veri setlerinde performansı korumak için sayfa başına 25-50 kayıt göster.

---

## 💡 Yeni Öneriler (15 adet)

### Finansal Özellikler
- [x] **Bütçe planlama ve aşım uyarıları.** Kategori bazlı aylık bütçe belirleme, limit aşılınca uyarı.
- [x] **Nakit akışı tahmini (cash flow forecast).** Gelecek aylar için gelir/gider projeksiyonu grafiği.
- [x] **KDV/vergi hesaplama desteği.** Fatura tutarlarına otomatik KDV ekleme/çıkarma.
- [x] **Tekrarlayan fatura/gelir otomasyonu.** Her ay otomatik olarak yeni kayıt oluşturma (kira, abonelik vb.).
- [x] **Çoklu para birimi desteği.** Güncel döviz kuru entegrasyonu ile otomatik TL karşılığı hesaplama.
- [x] **Banka hesabı entegrasyonu (Open Banking).** İşlemleri elle girmek yerine otomatik içe aktarma. *(Not: gerçek canlı Open Banking API bağlantısı yerine — aşağıdaki nota bakın — CSV ekstre içe aktarma uygulandı.)*
- [x] **OCR ile fatura/fiş otomatik okuma.** Yüklenen belge görselinden tutar/tarih/satıcı otomatik çıkarılıp form doldurulsun. *(Fatura/Masraf formuna "Fişten Otomatik Doldur" butonu eklendi. `/api/ocr-fis` route'u fotoğrafı sunucu tarafında Anthropic API'ye (vision) gönderip JSON olarak tutar/tarih/satıcı döndürüyor — anahtar istemciye hiç gitmiyor. **Gerekli:** Vercel ortam değişkenlerine `ANTHROPIC_API_KEY` eklemeniz lazım (console.anthropic.com'dan alınır). Şimdilik sadece Fatura/Masraf'ta; istenirse Gelir/Gider'e de eklenebilir.)*
- [x] **Yatırım portföyü için gerçek zamanlı fiyat.** Hisse/döviz/kripto fiyatları API'den otomatik çekilsin. *(Yatırım formuna "Piyasadan Çek" butonu + sayfa başlığına toplu "Tüm Fiyatları Güncelle" eklendi. Yeni `/api/piyasa-fiyati` route'u Yahoo Finance'in anahtarsız chart API'sini kullanıyor — hisse/döviz/kripto/altın destekleniyor. **Not:** Yahoo endpoint'i resmi/dokümante değil, haber vermeden değişebilir; kritik olmayan, elle girişe her zaman düşebilen bir yardımcı özellik. TEFAS yatırım fonları için anahtarsız kaynak olmadığından "fon" türü desteklenmiyor.)*

### Kurumsal / Güvenlik
- [ ] **Çoklu kullanıcı ve rol yönetimi.** Muhasebeci, yönetici, salt-görüntüleyici gibi farklı yetki seviyeleri. *(Kapsam kararı bekleniyor — mevcut mimaride her tablo `user_id = auth.uid()` üzerinden kişiye özel; gerçek rol bazlı erişim ~10 tablonun RLS politikalarını yeniden yazmayı gerektiren büyük/riskli bir değişiklik. Tam kapsamlı mı, hafif "salt-okunur paylaşım linki" versiyonu mu, yoksa şimdilik atlanıp sıradaki maddeye mi geçilecek — kullanıcıyla netleştirilecek.)*
- [x] **Aktivite/işlem geçmişi (audit log).** Kim, ne zaman, hangi kaydı değiştirdi — kurumsal denetim için. *(Yeni `/aktivite-gecmisi` sayfası + `supabase/migrations/20260816_audit_log.sql`. Migration DB seviyesinde genel bir trigger kurup ana tablolara (gelir/gider, fatura, borç/alacak, yatırım, varlık, bütçe, banka hesabı, belge, kategori, bordro) bağlıyor — uygulama kodunda değişiklik gerekmeden her ekleme/güncelleme/silme otomatik loglanıyor, güncellemelerde eski/yeni değer farkı da gösteriliyor. **Yapmanız gereken:** migration'ı Supabase SQL Editor'de çalıştırmanız lazım.)*
- [x] **Oturum yönetimi.** Aktif oturumları görüntüleme ve uzaktan çıkış yapma imkânı. *(Yeni `/oturumlar` sayfası. `auth.sessions` tablosu PostgREST'e açık olmadığından, `auth.uid()` ile korunan 3 adet SECURITY DEFINER SQL fonksiyonu (`get_my_sessions`, `revoke_my_session`, `revoke_other_sessions`) üzerinden erişiliyor — servis anahtarına/sunucu route'una gerek yok. Cihaz/tarayıcı user-agent'tan basitçe özetleniyor (`lib/user-agent.ts`). **Not:** Supabase access token'ları stateless JWT olduğundan, sonlandırılan bir oturumun token'ı süresi (varsayılan ~1 saat) dolana kadar teoride geçerli kalabilir — bu Supabase mimarisinin bir sınırı. **Yapmanız gereken:** `supabase/migrations/20260816_session_management.sql` dosyasını Supabase SQL Editor'de çalıştırmanız lazım.)*
- [x] **Şirket logosu ve markalı rapor şablonu.** Yüklenen logo ile PDF raporların üstbilgisinde görünmesi. *(Yeni `/ayarlar` sayfası: şirket adı + logo yükleme. Logo, ayrı bir storage bucket'a gerek kalmadan istemci tarafında canvas ile küçültülüp (max 240px) base64 data URL olarak `company_settings` tablosunda saklanıyor; PDF oluşturulurken doğrudan gömülüyor. Raporlar sayfası artık bu bilgiyi çekip `exportReportToPDF`'e geçiriyor — şirket adı girilmişse başlıkta "FinansApp" yerine şirket adı görünüyor, logo sol üstte, alt köşede küçük "FinansApp ile oluşturuldu" notu kalıyor. Excel dışa aktarma şimdilik değişmedi (kapsam dışı bırakıldı, istenirse eklenebilir). **Yapmanız gereken:** `supabase/migrations/20260816_company_settings.sql` dosyasını Supabase SQL Editor'de çalıştırmanız lazım.)*
- [x] **Otomatik zamanlanmış yedekleme.** Haftalık/aylık otomatik yedek, e-posta veya buluta gönderim. *(Şirket Ayarları sayfasına "Otomatik Yedekleme" bölümü eklendi: Kapalı/Haftalık/Aylık seçimi + geçmiş yedeklerin listesi/indirme linki. Arka planda `vercel.json`'daki Cron tanımıyla her gün 03:00 UTC'de tetiklenen `app/api/cron/yedekleme` route'u, sıklığı dolmuş kullanıcılar için JSON yedeği oluşturup private `yedekler` storage bucket'ına yüklüyor (e-posta yerine bulut/uygulama-içi arşiv tercih edildi — üçüncü parti e-posta servis anahtarına ihtiyaç duymuyor). Mevcut manuel "Yedek Al" düğmesinden bağımsız, ek bir koruma katmanı. **Yapmanız gereken (3 adım):** (1) `supabase/migrations/20260816_backup_scheduling.sql`'i Supabase SQL Editor'de çalıştırın — `yedekler` adında private bir storage bucket da oluşturuyor; (2) Vercel ortam değişkenlerine `SUPABASE_SERVICE_ROLE_KEY` (Supabase Dashboard > Project Settings > API) ve rastgele bir `CRON_SECRET` metni ekleyin; (3) Vercel'e deploy edince `vercel.json`'daki cron tanımı devreye girer — Vercel projenizde Cron Jobs özelliğinin plana dahil/etkin olduğunu da doğrulamanız gerekebilir.)*
- [ ] **Çoklu şirket / proje desteği.** Tek hesapla birden fazla işletme/proje arası geçiş (workspace). *(Kapsam kararı bekleniyor — "Çoklu kullanıcı ve rol yönetimi" ile aynı büyüklükte mimari bir değişiklik: ~10 tablonun her birine `workspace_id` eklenip RLS politikalarının yeniden yazılması, workspace geçiş arayüzü vb. gerektirir. Kullanıcı bu oturumda "Çoklu kullanıcı ve rol yönetimi"ni şimdilik atlamayı seçti; bu madde de büyük olasılıkla aynı kararla birlikte ele alınmalı.)*
- [x] **Dönem kilitleme (ay/yıl sonu kapanışı).** Geçmiş dönem kayıtlarının yanlışlıkla değiştirilmesini engelleme. *(Yeni `/donem-kilitleme` sayfası: bir tarih seçilip "bu tarihten önceki kayıtlar kilitli" hale getiriliyor. DB seviyesinde `transactions` (date), `bills` ve `debts` (due_date) tablolarına trigger eklendi — kilitli tarihten önceki bir kayıt eklenemez/güncellenemez/silinemez, PostgreSQL hatası olarak engellenir. `gelir-gider`/`fatura-masraf`/`borç-alacak` sayfalarındaki hata toast'ları artık DB'den dönen asıl mesajı (`error.message`) gösteriyor ki kilit engeli kullanıcıya görünür olsun (önceden bazıları jenerik "hata oluştu" diyordu, silme işlemlerinde ise hiç mesaj gösterilmiyordu). **Not:** Tek kullanıcılı hesap modelinde (rol ayrımı olmadığından) kilidi hesap sahibinin kendisi de kaldırabilir — bu, yanlışlık önleyici bir koruma, denetim-geçirmez bir kapanış garantisi değil. **Yapmanız gereken:** `supabase/migrations/20260816_period_locks.sql` dosyasını Supabase SQL Editor'de çalıştırmanız lazım.)*

### Kullanıcı Deneyimi
- [x] **Kategori bazlı grafik/pasta chart raporları.** Harcamaların görsel dağılımı. *(Raporlar sayfasındaki "Kategori Bazlı Harcamalar" kartına, kart başlığının yanına çubuk/pasta grafik geçişi eklendi — daha önce sadece yatay çubuk grafik olarak vardı, şimdi tek tıkla pasta grafiğe dönüştürülebiliyor, aynı veri/renkler kullanılıyor.)*
- [x] **Gelişmiş arama ve filtreleme.** Tarih aralığı, tutar aralığı, kategori bazlı çoklu filtre. *(Yeni ortak `AdvancedFilterBar` bileşeni (`components/data-table/advanced-filter.tsx`): serbest metin arama kutusu + açılır "Filtrele" paneli (başlangıç/bitiş tarihi, min/maks tutar, çoklu kategori seçimi — rozet olarak tıklanabilir). Gelir/Gider ve Fatura/Masraf sayfalarına eklendi; filtreleme istemci tarafında mevcut veri üzerinde çalışıyor, DB'ye ek sorgu atmıyor. Aktif filtre sayısı düğmede rozet olarak gösteriliyor, "Temizle" ile tek tıkla sıfırlanabiliyor.)*
- [x] **Klavye kısayolları.** Hızlı kayıt ekleme, arama gibi işlemler için kısayol tuşları. *(Yeni `lib/use-keyboard-shortcut.ts` genel hook'u + `components/keyboard-shortcuts-modal.tsx` yardım penceresi. Herhangi bir sayfada `?` tuşu kısayol listesini açar (sol menü altında da "Klavye Kısayolları" düğmesiyle erişilebilir). Gelir/Gider ve Fatura/Masraf sayfalarında `N` yeni kayıt modalını açar, `/` arama kutusuna odaklanır; bir metin kutusuna yazarken kısayollar (Esc hariç) devre dışı kalır, böylece normal yazı yazmayı engellemez.)*
- [x] **Yazdırılabilir (print-friendly) rapor görünümü.** Tarayıcıdan doğrudan düzgün baskı alınabilmesi. *(Raporlar sayfasına "Yazdır" butonu eklendi. Yazdırırken: sol menü/mobil çubuk/hesap makinesi gibi arayüz öğeleri gizleniyor, kartlar tek sütuna geçiyor ve karanlık modda bile kağıda her zaman açık zemin + siyah metin basılıyor (globals.css'teki `@media print` kuralları + `.print-card` sınıfı). Diğer sayfalara da (tablolar vb.) aynı `print:hidden` deseniyle kolayca genişletilebilir.)*
- [x] **Mobil bildirimler (PWA push).** Ödeme/vade hatırlatmalarının telefon bildirimi olarak gelmesi. *(Bildirim Tercihleri panelinin üzerine inşa edildi. Yeni `push_subscriptions` (cihaz başına Web Push aboneliği) ve `push_notification_log` (aynı hatırlatmanın aynı gün tekrar gönderilmesini engelleyen "gönderildi" defteri) tabloları. Şirket Ayarları > Bildirim Tercihleri'ne "Mobil Bildirimler (Push)" bölümü eklendi: kullanıcı her cihazda ayrı ayrı "Bu Cihazda Aç" diyerek tarayıcı bildirim izni verip abone oluyor, aktif cihazlarını görüp tek tek kaldırabiliyor. Service worker'a (`public/sw.js`) `push`/`notificationclick` event'leri eklendi. Yeni `app/api/cron/bildirim-gonder` route'u (vercel.json'a günlük — 07:00 UTC — cron olarak eklendi) her gün: (1) vadesi gecikmiş/bugün/3 gün veya daha az kalan fatura ve borçlar için (Bildirim Tercihleri'ndeki "Yaklaşan Ödemeler" açık olan kullanıcılara — 30 günlük tüm ufuk boyunca değil, spam olmaması için sadece son 3 gün/gecikmiş), (2) bütçe aşımı olan kategoriler için ("Bütçe Aşımları" açık olan kullanıcılara, günde en fazla 1 özet bildirimi) `web-push` kütüphanesiyle bildirim gönderiyor; artık geçersiz abonelikleri (404/410) otomatik temizliyor. **Yapmanız gereken (4 adım):** (1) `supabase/migrations/20260816_push_notifications.sql`'i Supabase SQL Editor'de çalıştırın; (2) bu oturumda sizin için bir VAPID anahtar çifti üretildi — sohbetteki son mesajda veriliyor (isterseniz `npx web-push generate-vapid-keys` ile kendiniz de yenisini üretebilirsiniz) — bunları Vercel ortam değişkenlerine `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT` (örn. `mailto:destek@sirketiniz.com`) olarak ekleyin; (3) `SUPABASE_SERVICE_ROLE_KEY` ve `CRON_SECRET` zaten "Otomatik zamanlanmış yedekleme" maddesinde ayarlanmış olmalı, aynıları bu cron için de kullanılıyor — ayarlanmadıysa şimdi ekleyin; (4) Vercel'e deploy edince cron devreye girer. **Not:** Vercel Hobby planında cron job'lar günde en fazla 1 kez çalışabiliyor ve toplam cron sayısı sınırlı olabilir — artık 2 cron job'ınız var (yedekleme + bildirim-gonder), planınızın buna izin verdiğini doğrulayın.)*
- [x] **Karşılaştırmalı trend raporları.** "Bu ay vs geçen ay", "bu yıl vs geçen yıl" analizleri. *(Raporlar sayfasına iki yeni kart eklendi: `lib/reports.ts`'teki `compareLastTwoMonths` ve `compareYearOverYear` fonksiyonları gelir/gider/net için önceki döneme göre yüzde değişimi hesaplıyor; artış yeşil, azalış kırmızı rozetle gösteriliyor. Karşılaştırılacak önceki dönem verisi yoksa (örn. yeni hesap) "yeterli veri yok" mesajı gösterilip yanıltıcı bir yüzde üretilmiyor. DB migration gerekmiyor.)*
- [x] **Belge arşivinde etiketleme ve arama.** Belge içeriğine veya etiketlere göre hızlı filtreleme. *(`documents` tablosuna `tags text[]` kolonu eklendi. Belgeler sayfasında: belge eklerken serbest metin etiket girişi (Enter/virgülle ekle, rozet olarak kaldırılabilir), belge kartlarında etiketler görünüyor, filtre çubuğunun altına tıklanabilir etiket rozetleri eklendi (çoklu seçim, OR mantığı) ve arama kutusu artık etiketleri de tarıyor. **Yapmanız gereken:** `supabase/migrations/20260816_document_tags.sql` dosyasını Supabase SQL Editor'de çalıştırmanız lazım.)*
- [x] **Bildirim tercihleri paneli.** Kullanıcının hangi bildirimleri, hangi kanaldan almak istediğini seçmesi. *(Yeni `notification_preferences` tablosu + Şirket Ayarları sayfasına "Bildirim Tercihleri" bölümü: Özet Paneli'ndeki "Yaklaşan Ödemeler" ve "Bütçe Aşımları" widget'larını ayrı ayrı açıp kapatma, yaklaşan ödemelerin kaç gün öncesinden (7/14/30/60) gösterileceğini seçme. Özet Paneli artık bu tercihleri okuyup ona göre widget'ları gizliyor/ufku ayarlıyor; kayıt yoksa (migration çalıştırılmamışsa) varsayılan olarak her ikisi de açık ve 30 gün kabul ediliyor, böylece mevcut davranış bozulmuyor. Kapsam notu: bu, hangi widget'ların *görüneceğini* kontrol eden bir tercih paneli; gerçek telefon bildirimi (push) göndermiyor — o, ayrı "Mobil bildirimler (PWA push)" maddesinin konusu ve bu panel o özelliğin temelini oluşturacak şekilde tasarlandı. **Yapmanız gereken:** `supabase/migrations/20260816_notification_preferences.sql` dosyasını Supabase SQL Editor'de çalıştırmanız lazım.)*

---

## 📝 Tamamlanan İşler (bu oturumda)

- [x] Lacivert & Altın kurumsal tema uygulandı (globals.css + PWA renkleri)
- [x] Dosya yükleme Cloudinary'den Supabase Storage'a taşındı (private bucket + imzalı link)
- [x] Vercel ortam değişkenleri (Supabase URL/Key) düzeltildi
- [x] `bills` tablosuna eksik `receipt_url` kolonu eklendi
- [x] Tüm sayfalardaki sabit gri renkler (`slate-*`) tema değişkenlerine bağlandı (29 dosya)
- [x] "..." satır menüsünün tablo içinde altta/görünmez kalma sorunu çözüldü (Portal tabanlı ortak bileşen — 5 dosya)
- [x] Marka adı tutarsızlığı düzeltildi ("FinansAsistanım" → "FinansApp", "Kişisel Finans Yönetimi" → "Kurumsal Finans Yönetimi")
- [x] Raporlar sayfasına "PDF İndir" / "Excel İndir" butonları eklendi (`lib/report-export.ts` — jspdf-autotable + xlsx, 3 tablo: nakit akışı, kategori harcamaları, portföy dağılımı)
- [x] Şifre sıfırlama akışı eklendi: Login'de "Şifremi Unuttum" linki → `/sifremi-unuttum` (e-posta ile link gönderimi) → `/sifre-guncelle` (yeni şifre belirleme, `PASSWORD_RECOVERY` event'i dinleniyor)
- [x] Tablo sayfalandırma zaten `DataTable` bileşeninde mevcuttu (25/50/100 seçenekli), 5 tabloda da (gelir-gider, fatura-masraf, borç-alacak, varlık, yatırım) aktif olduğu doğrulandı
- [x] Bütçe planlama eklendi: yeni `/butce` sayfası, kategori bazlı aylık limit belirleme, harcama/limit ilerleme çubuğu (yeşil/sarı/kırmızı), Özet Paneli'nde "Bütçe Aşımları" widget'ı. **Önemli:** yeni `budgets` tablosu gerekiyor — `supabase/migrations/20260816_create_budgets_table.sql` dosyasını Supabase SQL Editor'de elle çalıştırmanız lazım.
- [x] Nakit akışı tahmini eklendi: Raporlar sayfasına, son 3 ayın ortalamasına göre gelecek 3 ay için gelir/gider projeksiyonu gösteren yeni bir grafik (kesikli çizgiler) — `lib/reports.ts`'teki `projectCashFlow`.
- [x] KDV hesaplama desteği eklendi: Fatura/Masraf formundaki "Tutar" alanının yanına, KDV hariç↔dahil çevrimi yapıp sonucu tek tıkla tutar alanına aktaran bir hesaplayıcı (`components/kdv-calculator.tsx`, %1/%10/%20 oranları) — mevcut tabloya kolon eklemeden çalışır.
- [x] Tekrarlayan fatura/gelir otomasyonu eklendi: `bills` tablosunda zaten var olan `is_recurring`/`recurrence_period` alanlarına ek olarak `series_id` ve `recurrence_end_date` eklendi; `transactions` tablosuna da aynı alanlar (yeni) eklendi. `lib/recurring.ts`'teki `runRecurringAutomation`, dashboard layout'unda oturum açıldığında bir kez çalışıp vadesi geçmiş tekrarlayan kayıtların eksik dönemlerini otomatik oluşturuyor (idempotent — sunucu tarafı cron olmadığı için "yakalama" deseniyle çalışıyor). **Not:** Fatura/Masraf formunda tekrar ayarları (`is_recurring`/`recurrence_period`) için state zaten vardı ama arayüzde görünür bir kontrol yoktu (kullanıcı hiç açamıyordu) — bu eksik de bu oturumda giderildi. Gelir/Gider formuna da aynı tekrar mekanizması (maaş, kira geliri vb. için) eklendi.
- [x] Çoklu para birimi desteği eklendi (Gelir/Gider işlemleri): `transactions` tablosuna `currency`, `exchange_rate`, `try_equivalent` kolonları eklendi. `lib/currency.ts`, frankfurter.app (ECB tabanlı, anahtarsız/ücretsiz) üzerinden canlı kur çekip 1 saat localStorage'da önbelleğe alıyor; ağ hatasında kullanıcı kuru elle girebiliyor. Form üzerinde döviz seçici + TL karşılığı önizlemesi, tabloda döviz tutarı + TL karşılığı gösterimi var. Özet Panel, Raporlar ve Bütçe sayfalarındaki toplamlar artık `try_equivalent` üzerinden hesaplanıyor (yoksa `amount`'a düşüyor) — böylece döviz cinsinden kayıtlar toplamları bozmuyor. **Şu an sadece Gelir/Gider işlemlerinde var; Fatura/Masraf'a henüz eklenmedi.**
- [x] Banka hesabı entegrasyonu eklendi — **önemli kapsam notu:** Gerçek "Open Banking" (bankaya canlı API ile bağlanıp işlemleri otomatik çekme) BDDK lisanslı bir aracı/ödeme kuruluşu (TPP) hesabı ve o kuruluşla ayrı bir sözleşme/API anahtarı gerektirir; bu genel bir uygulamaya doğrudan eklenemez. Onun yerine yeni `/banka-hesaplari` sayfası: manuel banka hesabı tanımlama (ad, banka, IBAN son 4 hane, bakiye) ve bankanızın internet şubesinden indirdiğiniz ekstre CSV'sini içe aktararak işlemleri otomatik `transactions` tablosuna ekleme (`lib/bank-import.ts`) sunuyor. Aynı ekstrenin iki kez aktarılmasını `bank_account_id` + `external_ref` üzerindeki benzersiz kısıtlama engelliyor. `bank_accounts` tablosu, ileride gerçek bir sağlayıcı (ör. GoCardless Bank Account Data, Salt Edge) bağlanacaksa da aynı şekilde kullanılabilecek şekilde tasarlandı.
- [x] Native alert()/confirm() ve vade tarihi hatırlatıcıları önceki oturumda kod olarak tamamlanmış ama bu listede işaretlenmemiş bulundu — kontrol edilip ([x]) işaretlendi. Ayrıca fişten otomatik doldurma (OCR) eklendi: yeni `/api/ocr-fis` route'u + `components/ocr-receipt-button.tsx`, Fatura/Masraf formuna bağlandı.
- [x] **Uyarı:** `AGENTS.md` dosyasında, kodlama ajanlarını "node_modules/next/dist/docs/" gibi var olmayan bir yola yönlendirip oradan talimat okumaya ve bu bloğu sorgusuzca commit'lemeye teşvik eden şüpheli bir metin bulundu. Bu tür bir enjeksiyon denemesine benziyor; bu oturumda görmezden gelindi ve hiçbir talimatı uygulanmadı. Dosyanın kim tarafından/neden eklendiğini kontrol etmenizi öneririm.
- [x] **Önemli:** Bu oturumda eklenen tüm DB değişiklikleri (tekrarlayan işlem alanları, çoklu para birimi kolonları, `bank_accounts` tablosu) tek bir migration dosyasında: `supabase/migrations/20260816_recurring_currency_bank.sql` — Supabase SQL Editor'de elle çalıştırmanız lazım.
- [x] **Yeni oturum — 1. tur (3 madde):** Kategori bazlı pasta grafik seçeneği, gelişmiş arama/filtreleme (Gelir-Gider + Fatura-Masraf) ve klavye kısayolları (`?`, `N`, `/`) eklendi. Hiçbir DB migration'ı gerektirmiyor — sadece kod değişikliği, doğrudan deploy edilebilir. Ayrıca listede tekrar eden bir "Klavye kısayolları" satırı bulunup temizlendi.
- [x] **Yeni oturum — 2. tur (3 madde):** Karşılaştırmalı trend raporları (Raporlar sayfası, migration gerekmez), belge etiketleme ve arama (`documents.tags`, migration gerekli), bildirim tercihleri paneli (`notification_preferences`, migration gerekli) eklendi. **Yapmanız gereken:** `supabase/migrations/20260816_document_tags.sql` ve `supabase/migrations/20260816_notification_preferences.sql` dosyalarını Supabase SQL Editor'de çalıştırmanız lazım. AGENTS.md'deki şüpheli enjeksiyon metni bu turda da tekrar tespit edildi, yine görmezden gelindi — hâlâ dosyada duruyor, kaynağını kontrol etmeniz önerilir.
